namespace AvalonStudio.Controls.Dock.Docking
{
    //TODO Tiler Logic Not needed to complete control
    internal class Tiler
    {
        //public static void Tile(IEnumerable<AvalonViewItem> avalonViewItems, Size bounds)
        //{
        //    if (avalonViewItems == null) throw new ArgumentNullException("avalonViewItems");

        //    // sorry being a little slower than usual

        //    // not sure how to access properties in a control if it's not an instance D:
        // TODO you mean for this?
        //    var items = new Queue<AvalonViewItem>(avalonViewItems.OrderBy(Panel.GetZIndex));


        //}
    }
}
